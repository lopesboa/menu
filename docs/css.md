# CSS (Tailwind)

- Use Tailwind v4 with `@tailwindcss/vite`
- Sorted classes enforced via Biome nursery rule
- Common utilities in `@/utils/misc`: `cn()` for class merging
- Custom colors: `surface-*`, `primary-*`
