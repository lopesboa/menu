# TypeScript Standards

- Target: ES2022
- Strict mode enabled
- No unchecked side effect imports
- Path aliases: `@/` for `./src/*`, `~/` for `./*`
- Use explicit return types on exported functions
- Prefer `unknown` over `any`
- Use const assertions for immutable values
